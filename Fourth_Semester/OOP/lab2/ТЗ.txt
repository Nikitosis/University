Есть interface Algorithm с одним методом run.
Есть класс AlgorithmAnalizer со статическим методом runAlgo(Algorithm). Он использует рефлексию и обрабатывает алгоритмы(считает время выполнения, строит график 
скорости выполнения для разных входных данных)